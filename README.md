# To-Do-List-Web-Application
Create an interactive to-do list app with features to add, edit, delete, and mark tasks as completed. Implement JavaScript for dynamic UI updates and optionally use local storage for data persistence. This project introduces CRUD operations and client-side storage.
